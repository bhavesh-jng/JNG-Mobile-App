import 'package:flutter/material.dart';

// This file defines the color scheme for the app.
class AppColors {
  // The primary color scheme from the user's input.
  static const Color primaryLightest = Color(0xFFFAF7F0);
  static const Color primaryLighter = Color(0xFFD8D2C2);
  static const Color primaryNormal = Color(0xFFB17457);
  static const Color primaryDark = Color(0xFF4A4947);

  // A new vibrant color for animations and highlights.
  static const Color vibrantAccent = Color(0xFFF79E80);

  // Example of a secondary color scheme, if needed.
  static const Color secondary = Color(0xFF32A8A4);
  static const Color accent = Color(0xFFF7C59F);
}
